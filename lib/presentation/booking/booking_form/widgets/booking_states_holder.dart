import 'package:flutter/material.dart';

import 'booking_form.dart';

class BookingStatesHolder extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return BookingForm();
  }
}
